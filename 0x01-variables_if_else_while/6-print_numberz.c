#include <stdio.h>
/**
 * main - Entry point
 * description - a program that prints all single digit numbers
 *
 * Return: Always 0 (success)
*/
int main(void)
{
	int x;

	x = 0;
	while
		(x < 10) {
			putchar(x + '0');
			x++;
		}
	putchar('\n');
	return (0);

}

